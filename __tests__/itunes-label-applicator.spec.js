'use strict';

// Const ItunesLabelApplicator = require('../');

describe('iTunes (Record) Label Applicator', () => {
  it('reads an iTunes library', () => {

  });

  it('writes the name of a song\'s record label in the "Grouping" field');
});
